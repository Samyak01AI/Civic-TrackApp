<?xml version="1.0" encoding="utf-8"?>

<androidx.swiperefreshlayout.widget.SwipeRefreshLayout android:id="@+id/swipeRefresh"
    android:layout_width="wrap_content"
    android:layout_height="match_parent"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="16dp">

            <!-- Header Section -->
            <include android:id="@+id/header"
                layout="@layout/layout_home_header"/>

            <!-- Active Issues Horizontal List -->
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                style="@style/TextAppearance.CivicTrack.SectionHeader"
                android:text="@string/active_issues"/>

            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/rvIssues"
                android:layout_width="wrap_content"
                android:layout_height="180dp"
                android:layout_marginTop="8dp"
                android:orientation="horizontal"/>

            <!-- Quick Services Grid -->
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                style="@style/TextAppearance.CivicTrack.SectionHeader"
                android:text="@string/quick_services"
                android:layout_marginTop="24dp"/>

            <GridView
                android:id="@+id/gridServices"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:numColumns="2"
                android:horizontalSpacing="16dp"
                android:verticalSpacing="16dp"
                android:layout_marginTop="8dp"/>

            <!-- Progress Indicator -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:layout_marginTop="24dp">

                <TextView
                    style="@style/TextAppearance.CivicTrack.SectionHeader"
                    android:text="@string/progress"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"/>

                <com.google.android.material.progressindicator.LinearProgressIndicator
                    android:id="@+id/progressIndicator"
                    android:layout_width="match_parent"
                    android:layout_height="8dp"
                    android:layout_marginTop="8dp"
                    app:trackColor="@color/government_gray"
                    app:indicatorColor="@color/government_secondary"/>

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="@string/progress_subtext"
                    android:layout_gravity="end"
                    android:layout_marginTop="4dp"
                    android:textColor="@color/government_gray"/>
            </LinearLayout>
        </LinearLayout>
    </ScrollView>

    <androidx.drawerlayout.widget.DrawerLayout
        android:id="@+id/drawer_layout"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <!-- Main content -->
        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"/>

        <!-- Drawer panel (end/right side) -->
        <LinearLayout
            android:id="@+id/chat_panel"
            android:layout_width="320dp"
            android:layout_height="match_parent"
            android:layout_marginBottom="81dp"
            android:layout_gravity="end"
            android:background="#FFFFFF"
            android:orientation="vertical">

            <WebView
                android:id="@+id/chat_webview"
                android:layout_width="match_parent"
                android:layout_height="match_parent" />
        </LinearLayout>
    </androidx.drawerlayout.widget.DrawerLayout>
    </androidx.swiperefreshlayout.widget.SwipeRefreshLayout>
